# 33-国家-Country · 2026-08-28 03:20 dev cron 计划

> T4 昨夜未跑,plan 由 T2 03:20 dev cron 自造。
> 当日目标:1 个小变更、简洁可入库、双 push。

## 上下文

- T1 巡检 8/28 02:20 报告(`.Log/巡检-国家-20260828.md`)确认:Phase 0 完成度 3/8(README + index.html 骨架 + 解析脚本)。
- 巡检连续 5 天报同一项:`index.html` 仍是 Phase 0/Task 0.2 骨架页(2026-08-25 落,6699 字节),**未 fetch `data/countries.json` 渲染**;进度面板里 Task 0.3 还标"待启动"——与 `项目开发计划.md` 第 233 行 `[x]` 不同步,产生显示 drift。
- `data/countries.json` 已就位(17 国 / 11 分类 / 31 KB),零依赖纯 stdlib 生成,Web 渲染是 Phase 1 转段最小铺路动作。

## 当日变更(1 个)

**Phase 0/Task 0.4 · index.html 最小 Web 渲染**

- **目标**:`index.html` 静态页升级为"骨架 + 数据驱动"——`<script>` 块 `fetch('./data/countries.json')` 后,渲染:
  1. 进度面板 Task 0.3 状态从"待启动" → "已完成"(修正 HTML 与 `项目开发计划.md` 的显示 drift)
  2. stats 摘要:`unique_countries=17` `categories=11` `sections=41` `tables=30`(直接读 JSON 的 stats 段)
  3. 国别列表:17 国按名称排序,每行国家名 + 所在分类(取首条分类作代表)
- **零依赖**:不引 npm / 框架,纯 `<script>` ES2017,Fetch API 即可。
- **降级**:fetch 失败时,fallback 显示"数据未就位,请运行 `python3 scripts/md_to_country_db.py`",不破坏原骨架视觉。
- **不改**:`README.md` 立项书章节、`scripts/md_to_country_db.py` 解析器、`data/countries.json`(数据已经准确)、`.gitignore`、`.Log/` 巡检历史。

## 同步项

- `项目开发计划.md` §五 Phase 0 资产盘点:在原 3 项 `[x]` 之后追加 "Task 0.4 index.html 渲染 countries.json" 并勾上;§五顶部"进展"块追加一行 2026-08-28 03:20。
- `README.md` §六 资产盘点列表:同步追加新行(若该段也有 checkbox 列表)。

## 不做

- 不动解析器 / 不扩国别数 / 不加 Web 关系图 / 不写 FastAPI / 不动飞书 Bot。
- 不动 `国家顾问开发架构与计划.md`(8/26 已入库)。
- 不动 `.plan/` 历史(只有本篇新写)。

## 提交

- `git add -A`
- commit msg:`feat(country-web): Phase 0/Task 0.4 index.html 渲染 countries.json`
- 先 Gitee,后 GitHub
- 删 `.plan/20260828.md`
