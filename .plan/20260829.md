# 2026-08-29(dev-国家 cron 03:20 当日计划)

> 上下文:8/28 巡检 T1 报告 1 项 working tree 不干净(`.plan/20260828.md` 删未 commit)+ 1 项建议扩 parser。
> 今日目标:**小变更 + 简洁可入库**。

## 任务清单

1. **修 parser 两处缺陷**(scripts/md_to_country_db.py):
   - 默认 `--src` 改为"动态扫 `_CountryLib/` 下所有 `0X_*/` 目录"——目前硬编码只扫 01/02 两个,03-10 实际都有 md 但扫不到
   - glob 模式 `0*.md` 修成能扫到 `10_*.md`——目前 `0*` 不匹配 `10_*`
2. **重跑 parser** → stats 变化(sections 41 → 估计 170+,tables 30 → 估计 100+,因为 03-09 都有大量 ##/###/表格)
3. **同步 README §六** 描述("扫 9/10 目录"→"扫全册")
4. **勾选 `项目开发计划.md` §五**:在"FastAPI 骨架"前补一行"parser 覆盖度提升到全 10/10 目录"
5. **`git rm .plan/20260828.md`** 修 8/28 working tree 不干净
6. git add -A + commit + push

## 预期收益

- parser 覆盖度:2/10 → 10/10(完成 8/29 巡检 6 次上报的"扩 parser 覆盖"建议)
- countries.json stats 同步增长,Web 端读路径自动用上
- working tree 恢复干净
- 无需新增国别别名(03-10 实际只提及 中国/美国 2 个已知国,无需扩 COUNTRY_ALIASES)
